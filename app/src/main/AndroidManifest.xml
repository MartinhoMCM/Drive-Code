<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.in.bluetech.myapplication">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/main_image1"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@drawable/main_image"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Welcome_User" />
        <activity
            android:name=".DrawerActivity"
            android:label="@string/content"
            android:parentActivityName=".Welcome_User"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".contents_signal.SinaisPerigoIntroducaoActivity" />
        <activity android:name=".contents_signal.SPP1_Activity" />
        <activity android:name=".sinais_regulamentacao.priority_signal.SR_Activity" />
        <activity android:name=".sinais_regulamentacao.priority_signal.sinais_cedenciaPassagem.SCedenciaPassagemActivity" />
        <activity android:name=".sinais_cedencia.ScedenciaPassagemActivity" />
        <activity android:name=".authentication.LoginActivity" />
        <activity android:name=".authentication.RegisterActivity" />
        <activity android:name=".sinais_regulamentacao.priority_signal.sinais_obrigacao.SobrigacaoActivity" />
        <activity android:name=".sinais_regulamentacao.priority_signal.sinal_proibido.SproibicaoActivity" />
        <activity android:name=".sinais_regulamentacao.priority_signal.sinal_proibido.SROActivity" />
        <activity android:name=".sinais_regulamentacao.priority_signal.sinais_obrigacao.SOActivity" />
        <activity android:name=".general_concepts.gereralTrafficSignalActivity"></activity>
        <activity android:name=".quiz_code.QuizCodeActivity"></activity>
    </application>

</manifest>